Title:	Case insensitive search
Stat:	closed
Disp:	done
Prio:	high
Cats:	dev
Reqtrs:	
AssdTo:	stuart@friix.com

2007-02-24 00:00:00 stuart@friix.com
  srchres.pl -- Search on gloss is case-sensitive, probably should not be.

2007-08-09 00:00:00 stuart@friix.com
  Create a functional index on table gloss using "lower(txt)".
  In srchres.pl, create gloss WHERE clause like "lower(txt)=..." for gloss
  "is" search.
  Create a functional index on table gloss using "lower(txt
  varchar_pattern_ops)". (This index will be used in "LIKE "xxx%"
  searches.)
  In srchres.pl, create gloss WHERE clause like "lower(txt) LIKE ...".
  
  Note: "LIKE 'xxx%'" search still don't use index because of issue
  described in task-92.
